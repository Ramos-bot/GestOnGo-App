import React from 'react';
import ReactDOM from 'react-dom/client';
import AppNova from './AppNova.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <AppNova />
    </React.StrictMode>
);
